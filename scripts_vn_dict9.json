{
  "${buildings.filter((b) => b.status === 'active').length} đang hoạt động": "${buildings.filter((b) => b.status === 'active').length} active",
  "${h} giờ ${m} phút": "${h}h ${m}m",
  "${item.action} trên ${item.targetTable}": "${item.action} on ${item.targetTable}",
  "${m} phút": "${m}m",
  "${n.toLocaleString('vi-VN')} đ": "${n.toLocaleString('vi-VN')} ₫",
  "${overviewRes.data.counts.managers} quản lý": "${overviewRes.data.counts.managers} managers",
  "${overviewRes.data.counts.managers} quản lý / ${overviewRes.data.counts.staff} nhân viên": "${overviewRes.data.counts.managers} managers / ${overviewRes.data.counts.staff} staff",
  "${overviewRes.data.counts.staff} nhân sự vận hành": "${overviewRes.data.counts.staff} operations staff",
  "${paymentMethodDistribution.length} phương thức thanh toán": "${paymentMethodDistribution.length} payment methods",
  "${row.benefits!.length} ưu đãi": "${row.benefits!.length} benefits",
  "${row.graceDays ?? 7} ngày": "${row.graceDays ?? 7} days",
  "${row.hourlyRate.toLocaleString('vi-VN')} đ": "${row.hourlyRate.toLocaleString('vi-VN')} ₫",
  "${row.price.toLocaleString('vi-VN')} đ": "${row.price.toLocaleString('vi-VN')} ₫",
  "(c) ${new Date().getFullYear()} PBMS. Bảo lưu mọi quyền.": "(c) ${new Date().getFullYear()} PBMS. All rights reserved.",
  "Biển số \"${plate}\" đã được thêm.": "Plate \"${plate}\" has been added.",
  "Bị khóa (Thử lại sau ${Math.floor(lockTimeLeft / 60)}m ${lockTimeLeft % 60}s)": "Locked (retry in ${Math.floor(lockTimeLeft / 60)}m ${lockTimeLeft % 60}s)",
  "Ca làm việc (${items.length})": "Shifts (${items.length})",
  "Gia hạn gói \"${item.package.name}\" cho biển ${item.plateNumber}? Phí sẽ trừ từ ví.": "Renew package \"${item.package.name}\" for plate ${item.plateNumber}? The fee will be charged from your wallet.",
  "Gửi trực tiếp (${activeSessions.length})": "Walk-in (${activeSessions.length})",
  "Lỗi tải tầng: ${errorMsg}": "Error loading floors: ${errorMsg}",
  "Ngân hàng (BIN: ${qrInfo.bankBin})": "Bank (BIN: ${qrInfo.bankBin})",
  "Nhân viên (${items.length})": "Staff (${items.length})",
  "Nhật ký: ${currentTab ? targetLabel(currentTab) : ''}": "Logs: ${currentTab ? targetLabel(currentTab) : ''}",
  "NẠP TIỀN (${fmtMoney(selectedAmount)})": "TOP UP (${fmtMoney(selectedAmount)})",
  "Tòa nhà này không hỗ trợ loại xe ${vehicleType === 'car' ? 'car' : 'motorcycle'}.": "This building does not support ${vehicleType === 'car' ? 'car' : 'motorcycle'} vehicles.",
  "Tất cả (${items.length})": "All (${items.length})",
  "Tầng ${f.code || f.name || ''} (${f.availableSlots}/${f.totalSlots})": "Floor ${f.code || f.name || ''} (${f.availableSlots}/${f.totalSlots})",
  "Tầng ${floor}": "Floor ${floor}",
  "Tầng ${floor} • Ô ${slotCode}": "Floor ${floor} • Slot ${slotCode}",
  "Vô hiệu hóa chính sách \"${row.name}\"?": "Disable policy \"${row.name}\"?",
  "Xóa assignment cho ${row.staff?.fullName ?? 'nhân viên'}?": "Delete assignment for ${row.staff?.fullName ?? 'staff'}?",
  "Xóa ca ${row.code}?": "Delete shift ${row.code}?",
  "Xóa cổng ${row.code}?": "Delete gate ${row.code}?",
  "Xóa gói \"${row.name}\"?": "Delete package \"${row.name}\"?",
  "Xóa tầng ${row.code}? Tầng phải không có ô đỗ.": "Delete floor ${row.code}? The floor must have no slots.",
  "Xóa ô ${row.code}?": "Delete slot ${row.code}?",
  "Ô ${slotCode}": "Slot ${slotCode}",
  "Ô đỗ (${items.length})": "Slots (${items.length})",
  "Đang gửi (${items.filter((s) => s.status === 'active').length})": "Parking (${items.filter((s) => s.status === 'active').length})",
  "Đã thu phí & cho ra xe ${target.plateNumber}.": "Charged & released vehicle ${target.plateNumber}.",
  "Đã quét mã đặt chỗ: ${code}": "Scanned reservation code: ${code}",
  "Đã tạo phiên gửi xe cho biển số ${currentPlate} thành công.": "Parking session created for plate ${currentPlate}.",
  "Đã xong (${items.filter((s) => s.status === 'completed').length})": "Done (${items.filter((s) => s.status === 'completed').length})",
  "Đăng ký gói \"${selectedPkg.name}\" thành công!": "Subscribed to \"${selectedPkg.name}\" successfully!",
  "Đặt chỗ (${filtered.length})": "Reservations (${filtered.length})",
  "Đặt chỗ (${paidReservations.length})": "Reservations (${paidReservations.length})"
}
